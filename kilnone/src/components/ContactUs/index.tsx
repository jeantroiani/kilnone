import React from 'react';
import styles from './styles.module.scss';
import globalStyles from '../Styles/styles.module.scss';

export const ContactUs = () => {

    return (
        <section > 
            <h1 className={globalStyles.sectionMainTitle}>Contact Us</h1>
           
        </section>
     
    );

}
